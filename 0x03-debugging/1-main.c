#include "main.h"

/**
 * largest_number - returns the largest number of 3 integer
 * @a: fisrt integer
 * @b: secodnd integer
 * @c: third integer
 * Return: largest integer
 */
int largest_number(int a, int b, int c)
{
	int largest;

	if(a > b && a > c )
	{
		largest = a;
	}
	else if (c > b)
	{
		largest = c;
	}
	else largest b;

return (largest);
}
