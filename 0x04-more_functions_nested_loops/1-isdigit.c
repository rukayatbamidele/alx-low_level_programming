#include "main.h"

/**
 * _isdigit - Checks for a digit
 * @c:The character to be checked
 * Return: 0 for digit character or 1 for anything else
 */
int _isdigit(int c)
{
if (c >= 48 && c <= 57)
{
return (0);
}
return (1);
}
